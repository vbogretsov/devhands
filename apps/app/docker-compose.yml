services:
  app-1:
    image: vbogretsov/app:1
    build: .
    environment:
      DB_HOST: postgres
      DB_USER: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_POOL_SIZE: 1000
      JWT_KEY: ${JWT_KEY}
      LOG_LEVEL: DEBUG
