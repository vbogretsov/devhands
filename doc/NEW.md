# Что я узнал

- coordinated omission!!!
- load average == number of tasks in scheduler queue
- getrusage
- в большой системе тормозит одинаково всё если она хорошо спроектирована
- аггресивное кэширование статики может помочь снизить нагрузку на порядки
- при приблежении к максимуму запросов на ядро происходит резкий взлёт latency
- если запрос очень лёгкий то разница от проксирования уменьшаяет rps мнимум в двое
- алгоритм масштабирования (Mar 24)
- maxmind
- кэш должен быть устойчивым к сбросу!!!
- пример стратегии инвалидации кэша (battle tested) (Mar 24)
- массовая кэш инвалидация, как подходить (рандомные задержки)
- Garage sharding
- docker overhead: network, fs, logging, seccomp, apparmor
- смотерть на связность с разных точек зрения: данные, сеть, безопасность, время
- registrator для саг!!!
- когда подходим к 60-70% CPU система перегружена и latency гафики могут сильно поплыть из-за того что сами измерения непраильне
- Redis Hash-Slots
